<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>dev.rono</groupId>
    <artifactId>igniscore-parent</artifactId>
    <version>1.0.0</version>
  </parent>

  <artifactId>dist</artifactId>
  <packaging>jar</packaging>

  <name>dist</name>

  <dependencies>
    <dependency>
      <groupId>dev.rono</groupId>
      <artifactId>plugin</artifactId>
    </dependency>
  </dependencies>

  <build>
    <finalName>igniscore</finalName>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <execution>
            <id>unpack-plugin</id>
            <phase>prepare-package</phase>
            <goals>
              <goal>unpack</goal>
            </goals>
            <configuration>
              <artifactItems>
                <artifactItem>
                  <groupId>dev.rono</groupId>
                  <artifactId>plugin</artifactId>
                  <version>${project.version}</version>
                  <outputDirectory>${project.build.outputDirectory}</outputDirectory>
                </artifactItem>
              </artifactItems>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <version>3.1.0</version>
        <executions>
          <execution>
            <id>bundle-block-extensions</id>
            <phase>prepare-package</phase>
            <goals>
              <goal>run</goal>
            </goals>
            <configuration>
              <target>
                <mkdir dir="${project.build.outputDirectory}/bundled/blocks"/>
                <copy todir="${project.build.outputDirectory}/bundled/blocks" flatten="true">
                  <fileset dir="${project.basedir}/../blocks">
                    <include name="*/target/*.jar"/>
                    <exclude name="*/target/original-*.jar"/>
                  </fileset>
                </copy>
                <mkdir dir="${project.build.outputDirectory}/bundled/items"/>
                <copy todir="${project.build.outputDirectory}/bundled/items" flatten="true">
                  <fileset dir="${project.basedir}/../items">
                    <include name="*/target/*.jar"/>
                    <exclude name="*/target/original-*.jar"/>
                  </fileset>
                </copy>
              </target>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <version>3.4.2</version>
        <configuration>
          <forceCreation>true</forceCreation>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
