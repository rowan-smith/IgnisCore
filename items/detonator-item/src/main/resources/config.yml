id: detonator

display:
  title: "&6Detonator"
  description:
    - "&7Left-click a remote bomb to link"
    - "&7Right-click to detonate linked bombs"

item:
  base_material: blaze_rod

textures:
  icon: icon.png

behavior:
  strategy: detonator
  strategy_name: Remote Detonator
  custom_data:
    target_block: remote-bomb
    max_links: 16

interactions:
  left_click:
    action: assign_bomb
  right_click:
    action: detonate_linked
