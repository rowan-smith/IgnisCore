package dev.rono.igniscore.paper.command;

import dev.rono.igniscore.command.PluginCommandHandler;

public interface PaperIgnisCommandHost {
    void bindPaperIgnisCommand(PluginCommandHandler handler);
}
